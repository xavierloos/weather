import React from 'react';
import styled from '@emotion/styled';

const Icon = (props) => {
    const Icon = styled.img`
        width:40%;
        margin:auto;
    `
    return (
        <Icon className="icon" src="" alt="Icon" />
    );
}
export default Icon;